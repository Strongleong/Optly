name: Build and Test

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
    paths:
      - '**.c'
      - '**.h'
      - 'build.sh'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
        - uses: actions/checkout@v4
        - run: ./build.sh && ./out/tests
